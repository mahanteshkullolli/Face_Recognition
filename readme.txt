*version
	->python 3.9.0
	->pip install "cmake==3.21.4"
	->pip install -v "dlib==19.22.1"
	->pip install "face-recognition==1.3.0"
	->vscode desktop development with c++(build tool)


*modules to import

	->numpy
	->opencv
	->csv
	->face_recognition
	->datetime

=>code by mahantesh_m_kullolli
